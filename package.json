{
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "client": "npm run dev --prefix frontend",
    "server": "npm run dev --prefix backend",
    "dev": "concurrently -n \"CLIENT,SERVER\" -c \"blue,yellow\" \"npm run client\" \"npm run server\""
  }
}
